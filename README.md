## Instalasi

```sh
python -m venv venv  # Buat virtual environment
venv\Scripts\activate  # (Windows) Aktifkan venv
# source venv/bin/activate  # (Linux/macOS)
pip install -r requirements.txt  # Install semua dependensi

run:
python ascii_draw.py
